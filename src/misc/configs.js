/* global Configs */

export default new Configs({
	walletJSON: null,
	messages: {},
	showNotifications: true,
	chatSettings: {},
	// Confirmed transactions only.
	transactions: {
		confirmed: [],
	},
});
